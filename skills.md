# Applied Skills

- TypeScript: strict typing, narrow DTOs, explicit unions.
- React Native: modal-first flows, controlled inputs, direct screen logic.
- Supabase: CRUD queries, filtering, ordering, simple RLS.
- Error handling: basic user-safe messages, no silent failures.
- Git workflow: `commit-slices` for splitting diffs into N coherent commits with optional instructions.
